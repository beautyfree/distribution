services:
  db:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: distribution
      POSTGRES_PASSWORD: distribution
      POSTGRES_DB: distribution
    volumes:
      - distribution_pg:/var/lib/postgresql/data

volumes:
  distribution_pg:
